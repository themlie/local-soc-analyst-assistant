# Runtime dependencies — what the assistant needs to analyse logs.
#
# Direct imports only. This file used to be a `pip freeze` dump of 60 packages, which
# pinned transitive versions nobody chose and hid what the project actually depends on.
# Versions are pinned exactly: a security tool should be reproducible, and an
# unexpected dependency upgrade is a change nobody reviewed.
#
# Refresh a pin deliberately, then re-run: python -m pytest tests/ -q

foundry-local-sdk==1.2.3   # on-device LLM and embedding runtime (common/llm.py)
evtx==0.12.1               # real Windows EVTX parsing (ingest/win_evtx.py)
streamlit==1.58.0          # web interface (ui/app.py)

# Not required by the pipeline: numpy is used only by the optional experiment in
# experiments/attack_semantic_search.py. Install it from requirements-dev.txt.
